---
post_title: "fix: finish issue #32 - real QuickGrid, glossary, idempotency tests, seed data"
author1: team-squad
post_slug: "v0.1.18-pr-51"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.1.18
  - automation
ai_note: "Generated by release automation using PR title/body"
summary: "Release notes seed for v0.1.18 from PR #51."
post_date: "2026-08-21"
---
## fix: finish issue #32 - real QuickGrid, glossary, idempotency tests, seed data

- **Release tag:** `v0.1.18`
- **Source PR:** [#51](https://github.com/mpaulosky/Articles/pull/51)

### PR description

## Summary
- Rewrites `ArticlesPage.razor` to actually use `Microsoft.AspNetCore.Components.QuickGrid` as #32 specified, instead of the hand-rolled `<table>` that shipped in #37/#49 — `PropertyColumn`/`TemplateColumn` with per-column `ColumnOptions` filter popups, `GridSort` for sorting, and the built-in `Paginator`. Zebra striping moves to plain `nth-child` CSS since QuickGrid's `RowClass` callback has no row index to key off.
- Adds the `Article`/`Published`/`Draft`/`Archived` glossary terms to `CONTEXT.md` — the spec assumed these were already written ahead of #32, but they never were.
- Adds the idempotent Archive/Unarchive cases to `ArticleFeatureHandlerTests` that the spec's Testing Decisions called for at the handler seam (only domain-entity-level coverage existed).
- Fixes Article seed data drift: none of the three existing seed articles set `isArchived` after the field was added, so the archiving feature had no seeded example to exercise. All three now set it explicitly, and a fourth, archived seed article is added.

Closes #32

## Why
A completeness review of #32 found these gaps survived across all 6 closed sub-issues (#33-#38) individually being "done" — the QuickGrid requirement and CONTEXT.md precondition were both explicit, testable items in the parent spec that no single sub-issue's scope happened to cover.

## Test plan
- [x] `dotnet build Articles.slnx` — 0 warnings, 0 errors
- [x] Full test suite: 434 tests passing (193 Web.Tests, 101 Web.UI.Tests, 94 Domain.Tests, 13 Architecture.Tests, 33 AppHost.Tests), including the 33 rewritten `ArticlesPageTests` driving real QuickGrid markup (col-title sort buttons, col-options filter popups, padded-page-aware pagination)
- [x] Verified QuickGrid's stylesheet auto-bundles into the already-linked `Web.styles.css` with no manual `<link>` needed
- [ ] Manual browser verification not possible in this environment (no Auth0 dev-login bypass configured), same limitation noted in #49

🤖 Generated with [Claude Code](https://claude.com/claude-code)

