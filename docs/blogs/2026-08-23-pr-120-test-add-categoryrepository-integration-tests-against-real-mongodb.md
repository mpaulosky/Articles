---
post_title: "test: add CategoryRepository integration tests against real MongoDB"
author1: team-squad
post_slug: "v0.1.45-pr-120"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.1.45
  - automation
ai_note: "Generated by release automation using PR title/body"
summary: "Release notes seed for v0.1.45 from PR #120."
post_date: "2026-08-23"
---
## test: add CategoryRepository integration tests against real MongoDB

- **Release tag:** `v0.1.45`
- **Source PR:** [#120](https://github.com/mpaulosky/Articles/pull/120)

### PR description

## Summary
- Adds `tests/Web.Integration.Tests/Data/CategoryRepositoryTests.cs` covering `CategoryRepository`'s full surface (Add, GetAll, GetById, Update, plus `UpdateAsync`'s detached-entity handling) against the real `MongoContainerFixture` — no mocked database, following the pattern set by `ArticleRepositoryTests`.
- Fixes `MongoContainerFixture.CreateContext` to ignore EF Core's `ManyServiceProvidersCreatedWarning`: each test method's unique database name intentionally builds its own `DbContextOptions`, and the assembly now creates enough of them across all test classes to trip that warning-as-error. Left unfixed, this would also hit the upcoming handler-level integration test tickets.

Part of #112 (child of #109).

## Test plan
- [x] `dotnet build` — solution builds clean
- [x] `dotnet test tests/Web.Integration.Tests` — 23/23 pass against the real MongoDB container
- [x] Pre-push gate (full solution test suite) green

