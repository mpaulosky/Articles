---
post_title: "test: add ArticleFeatureHandler integration tests against the real mediator pipeline"
author1: team-squad
post_slug: "v0.1.46-pr-122"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.1.46
  - automation
ai_note: "Generated by release automation using PR title/body"
summary: "Release notes seed for v0.1.46 from PR #122."
post_date: "2026-08-23"
---
## test: add ArticleFeatureHandler integration tests against the real mediator pipeline

- **Release tag:** `v0.1.46`
- **Source PR:** [#122](https://github.com/mpaulosky/Articles/pull/122)

### PR description

## Summary
- Dispatches all 10 `ArticleFeatureHandler` requests through the real `IMediator` (`AddMyMediator` + `LoggingBehavior`, matching `Program.cs`) against a real MongoDB TestContainers database, instead of calling the handler directly as `Web.Tests` does with EF InMemory.
- Adds a reusable `MediatorTestHost` fixture (builds a fresh DI container per test method, mirroring the existing per-test-method unique-database convention) that `CategoryFeatureHandler`'s and `UserManagementHandler`'s tickets will build on.
- Grants `Web.Integration.Tests` the `InternalsVisibleTo` access needed to reference the handler's internal commands/handler/validators.

Resolves [ArticleFeatureHandler integration tests](https://github.com/mpaulosky/Articles/issues/113), part of the [Integration testing for the Articles project](https://github.com/mpaulosky/Articles/issues/109) map.

## Test plan
- [x] `dotnet build tests/Web.Integration.Tests/Web.Integration.Tests.csproj` succeeds
- [x] `dotnet test tests/Web.Integration.Tests/Web.Integration.Tests.csproj` — 36/36 pass (13 new mediator-pipeline facts + 23 existing repository facts) against a real MongoDB TestContainers instance
- [x] `dotnet build src/Web/Web.csproj` succeeds after the `InternalsVisibleTo` addition

🤖 Generated with [Claude Code](https://claude.com/claude-code)

