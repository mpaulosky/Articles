---
post_title: "test: add CategoryFeatureHandler integration tests against the real mediator pipeline"
author1: team-squad
post_slug: "v0.1.47-pr-124"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.1.47
  - automation
ai_note: "Generated by release automation using PR title/body"
summary: "Release notes seed for v0.1.47 from PR #124."
post_date: "2026-08-23"
---
## test: add CategoryFeatureHandler integration tests against the real mediator pipeline

- **Release tag:** `v0.1.47`
- **Source PR:** [#124](https://github.com/mpaulosky/Articles/pull/124)

### PR description

## Summary
- Dispatches all six `CategoryFeatureHandler` requests (Create, GetCategories, GetCategoryById, Update, Archive, Unarchive) through the real `IMediator` against a real MongoDB container, following the pattern set by the `ArticleFeatureHandler` integration tests.
- Uses a shorter database-name prefix than the full class name so the generated per-test database name stays under MongoDB's 63-character limit.

Closes #114

## Test plan
- [x] `dotnet test tests/Web.Integration.Tests/Web.Integration.Tests.csproj` — 46/46 passed
- [x] Pre-push gate (full solution build + test suite) passed

