---
post_title: "test: add UserManagementHandler integration tests against the real mediator pipeline"
author1: team-squad
post_slug: "v0.1.48-pr-126"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.1.48
  - automation
ai_note: "Generated by release automation using PR title/body"
summary: "Release notes seed for v0.1.48 from PR #126."
post_date: "2026-08-23"
---
## test: add UserManagementHandler integration tests against the real mediator pipeline

- **Release tag:** `v0.1.48`
- **Source PR:** [#126](https://github.com/mpaulosky/Articles/pull/126)

### PR description

## Summary
- Resolves wayfinder ticket [UserManagementHandler integration tests](https://github.com/mpaulosky/Articles/issues/115) on map [Integration testing for the Articles project](https://github.com/mpaulosky/Articles/issues/109)
- Adds `tests/Web.Integration.Tests/Features/UserManagement/Handlers/UserManagementHandlerTests.cs` dispatching all four `UserManagementHandler` requests (GetUsersWithRolesQuery, GetAvailableRolesQuery, AssignRoleCommand, RemoveRoleCommand) through the real `IMediator` pipeline
- Unlike `ArticleFeatureHandler`/`CategoryFeatureHandler`, this handler has no MongoDB dependency — it calls the Auth0 Management API. Adds a new lightweight, non-Mongo `UserManagementTestHost` fixture that wires the real `IMediator` (`AddMyMediator` + `LoggingBehavior`) and the real `UserManagementCacheService` (real `IMemoryCache` + in-memory `IDistributedCache`), substituting only `IManagementApiClientFactory` via NSubstitute since a live Auth0 tenant isn't available in CI
- Trimmed to one representative happy-path fact per request type; factory-failure branches stay covered at the unit level in `Web.Tests`
- Adds `NSubstitute` as a package reference to `Web.Integration.Tests`

## Test plan
- [x] `dotnet build tests/Web.Integration.Tests/Web.Integration.Tests.csproj` — builds clean
- [x] `dotnet test tests/Web.Integration.Tests/Web.Integration.Tests.csproj` — 50/50 pass (including existing MongoDB container tests)
- [x] `dotnet build Articles.slnx` — whole solution builds clean
- [x] pre-push gate test suite passed locally

🤖 Generated with [Claude Code](https://claude.com/claude-code)

