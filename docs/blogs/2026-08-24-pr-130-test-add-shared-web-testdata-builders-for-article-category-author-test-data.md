---
post_title: "test: add shared Web.TestData builders for Article/Category/Author test data"
author1: team-squad
post_slug: "v0.1.49-pr-130"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.1.49
  - automation
ai_note: "Generated by release automation using PR title/body"
summary: "Release notes seed for v0.1.49 from PR #130."
post_date: "2026-08-24"
---
## test: add shared Web.TestData builders for Article/Category/Author test data

- **Release tag:** `v0.1.49`
- **Source PR:** [#130](https://github.com/mpaulosky/Articles/pull/130)

### PR description

## Summary
- Resolves #129 (part of #109): extracts static factory-method builders for `CreateArticleCommand`, `UpdateArticleCommand`, `CreateCategoryCommand`, `UpdateCategoryCommand`, `CategoryDto`, and `AuthorDto` into a new `testutils/Web.TestData` project shared by `Web.Tests` and `Web.Integration.Tests`.
- Placed outside `tests/` (see [ADR-0002](docs/adr/0002-shared-test-data-outside-tests-folder.md)) because `squad-ci.yml`'s `discover-tests` job globs every `tests/**/*.csproj` unconditionally, and a non-test class library there would break that matrix.
- One worked-example refactor per project (`ArticleFeatureHandlerTests.cs` in each) proves the builders are usable in both testing styles; no other existing tests were retrofitted, per the decision recorded on #129.

## Test plan
- [x] `dotnet build Articles.slnx` succeeds
- [x] `Web.Tests` full suite passes (317/317)
- [x] `Web.Integration.Tests` full suite passes (50/50, against real MongoDB TestContainers)
- [x] `Architecture.Tests` passes (13/13) — unaffected, since it only asserts on `src/`
- [x] Pre-push gate ran the whole solution's test suite locally and passed

🤖 Generated with [Claude Code](https://claude.com/claude-code)

