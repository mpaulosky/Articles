---
post_title: "feat: implement User role E2E smoke tests"
author1: team-squad
post_slug: "v0.1.69-pr-192"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.1.69
  - automation
ai_note: "Generated by release automation using PR title/body"
summary: "Release notes seed for v0.1.69 from PR #192."
post_date: "2026-08-26"
---
## feat: implement User role E2E smoke tests

- **Release tag:** `v0.1.69`
- **Source PR:** [#192](https://github.com/mpaulosky/Articles/pull/192)

### PR description

## Summary
- Implements the plain authenticated User-role thin E2E checklist decided on #162, expanding the wayfinder map at #156 (resolves #165): admin-only nav links absent, redirected to `/not-authorized` navigating to `/admin/users`, and User can view a published article Admin publishes as shared setup - the one action available to it since it cannot create, edit, or archive.
- New `UserAuthFixture` mirrors `AdminAuthFixture`/`AuthorAuthFixture`, wired into `E2ETestCollectionDefinition`.
- Extracted `RoleTestHelpers` (`GotoAndWaitForCircuitAsync`, `AsyncDisposeAction`, `CreateCategoryAsync`, `CreateArticleAsync`) out of `AdminRoleTests`/`AuthorRoleTests`, which had duplicated this near-verbatim - per the code-review finding on PR #190 that flagged this as worth fixing before a third role-test file (this one) compounded it.
- Surfaced a real bug: the `/articles` list is paginated (10/page, sorted by title) and every E2E run leaves its throwaway articles behind with no cleanup, so a freshly created article can fall off the default page once enough runs accumulate data locally. Added `FindArticleRowAsync`, which searches by the article's unique per-run title before asserting on its row, fixing both the new Publish flow and the pre-existing Admin archive test (which hit the identical failure once local test data had piled up during this work).

**Stacked on #190** (targets `squad/164-author-role-e2e-tests`, not `main`) since the `/not-authorized` redirect test depends on that branch's `AccessDeniedPath` fix, which isn't in `main` yet. Will retarget to `main` automatically once #190 merges.

## Test plan
- [x] `dotnet test tests/Web.E2E.Tests`: 10/10 pass (run twice against the real tenant/DB to check for flakiness)
- [x] `dotnet test tests/Web.Tests`: 349/349
- [x] `dotnet test tests/Web.UI.Tests`: 150/150

🤖 Generated with [Claude Code](https://claude.com/claude-code)

