---
post_title: "feat: show release version and live releases list on docs site"
author1: team-squad
post_slug: "v0.1.11-pr-28"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.1.11
  - automation
ai_note: "Generated by release automation using PR title/body"
summary: "Release notes seed for v0.1.11 from PR #28."
post_date: "2026-08-21"
---
## feat: show release version and live releases list on docs site

- **Release tag:** `v0.1.11`
- **Source PR:** [#28](https://github.com/mpaulosky/Articles/pull/28)

### PR description

## Summary

- Retires the stale, hand-written "Release notes" section on `docs/index.html` (it was frozen at an old date while the repo had moved on several releases).
- Adds a version badge next to the "Articles" brand in the header, showing the latest release tag and linking to it on GitHub.
- Adds a new "Releases" section at the bottom of the page (after the blogs section), listing the 10 most recent releases as date + tag + link, with a "View all releases on GitHub" fallback link.
- Both the badge and the list are populated by a client-side fetch to the public GitHub REST API at page load — `.github/workflows/squad-release.yml` is untouched.
- Loading placeholder while fetching; graceful fallback link if the fetch fails.
- Records the design decision in `CONTEXT.md` and `docs/adr/0001-live-fetched-release-list.md`.

## Test plan

- [x] Verified in a headless Chromium session: version badge resolves to the latest tag and links correctly, Releases section renders 10 items with working links, nav updated, zero console errors.
- [x] Ran the two-axis `/code-review` (Standards + Spec) and applied the two minor cleanups it flagged (deduped the badge fallback logic, added a `MAX_RELEASES` constant).
- [x] Local pre-push gate (build + full test suite) passed.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

