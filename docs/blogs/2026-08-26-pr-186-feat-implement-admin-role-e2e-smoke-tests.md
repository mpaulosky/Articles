---
post_title: "feat: implement Admin role E2E smoke tests"
author1: team-squad
post_slug: "v0.1.66-pr-186"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.1.66
  - automation
ai_note: "Generated by release automation using PR title/body"
summary: "Release notes seed for v0.1.66 from PR #186."
post_date: "2026-08-26"
---
## feat: implement Admin role E2E smoke tests

- **Release tag:** `v0.1.66`
- **Source PR:** [#186](https://github.com/mpaulosky/Articles/pull/186)

### PR description

## Summary
- Expands the `AdminSmokeTests` prototype from #159 into the thin per-role checklist decided on #162: admin-only nav links are visible, Admin lands on the Admin-gated Manage Roles page, and Admin can create and archive a throwaway article through the real UI — the one action gated Admin-only via `ArticleAuthorizationService.CanArchiveArticle`.
- All test data is created live per run through the real UI; nothing is seeded.
- Adds `id` attributes to `ArticleForm`'s Title/Category fields and `CategoryForm`'s Name/Description fields so Playwright can target them reliably (no behavior change).
- Adds a `GotoAndWaitForCircuitAsync` helper that waits for the Blazor Server SignalR circuit to connect after navigation before interacting — a click sent before the circuit is up is silently dropped even once the target element is visible/enabled.

Resolves #163.

## Test plan
- [x] `dotnet test tests/Web.E2E.Tests` — 4/4 pass against the real Auth0 tenant and dev database (run twice to confirm no flakiness)
- [x] `dotnet test tests/Web.Tests` — 349/349 pass
- [x] `dotnet test tests/Web.UI.Tests` — 150/150 pass
- [x] Pre-push gate (full suite) passed

🤖 Generated with [Claude Code](https://claude.com/claude-code)

